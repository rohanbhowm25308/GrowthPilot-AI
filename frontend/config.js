// If you run the frontend separately from the Flask backend, set API_BASE
// to the backend's URL, e.g. "http://localhost:5050". Leave "" if Flask is
// serving this file directly (recommended — same-origin, no CORS needed).
const API_BASE = "";
